/**
 * @file lv_conf.h
 * Configuration file for LVGL v9 for the ESP32-S3 Pendant project.
 */

#ifndef LV_CONF_H
#define LV_CONF_H

#include <stdint.h>

/*====================
   Graphical settings
 *====================*/

/* Maximal horizontal and vertical resolution to support by the library.*/
#define LV_HOR_RES_MAX (480) // <-- Set for your 480x320 display
#define LV_VER_RES_MAX (320) // <-- Set for your 480x320 display
#define LV_COLOR_DEPTH 16    // <-- Set to 16-bit for RGB565 color
#define LV_COLOR_16_SWAP 1   // <-- Often needed on ESP32 for correct colors

/*==================
 * Performance
 *==================*/

/* Set the default tick period of the scene background task.
 * A shorter period means faster updates but more CPU usage. */
#define LV_DISP_DEF_REFR_PERIOD 30

/*==================
   HAL settings
 *==================*/

/*Default read period of input devices*/
#define LV_INDEV_DEF_READ_PERIOD 50

/*================
 * Font settings
 *===============*/

/* Montserrat fonts with ASCII range and some symbols */
#define LV_FONT_MONTSERRAT_12 1
#define LV_FONT_MONTSERRAT_14 1 // <-- Used in your UI
#define LV_FONT_MONTSERRAT_20 1 // <-- Used in your UI
#define LV_FONT_MONTSERRAT_30 1 // <-- Used in your UI
#define LV_FONT_MONTSERRAT_40 1 // <-- Used in your UI

/* Default font */
#define LV_FONT_DEFAULT &lv_font_montserrat_14

/* Enable built-in FontAwesome-like symbols */
#define LV_USE_FONT_SYMBOLS_BUILT_IN 1 // <-- Used for WiFi/Battery icons

/*================
 * Widget settings
 *================*/

/* --- Base objects --- */
#define LV_USE_ARC 1    // <-- Used in your UI
#define LV_USE_BTN 1    // <-- Used in your UI
#define LV_USE_LABEL 1  // <-- Used in your UI
#define LV_USE_SWITCH 1 // <-- Used in your UI
#define LV_USE_WIN 1    // <-- Used in your UI

/* --- Extra widgets --- */
#define LV_USE_DROPDOWN 1 // <-- Used in your UI
#define LV_USE_ROLLER 1   // <-- Used in your UI
#define LV_USE_TABVIEW 1  // <-- Used in your UI
#define LV_USE_TEXTAREA 1 // <-- Used in your UI
#define LV_USE_BAR 1
#define LV_USE_SLIDER 1
#define LV_USE_CHECKBOX 1
#define LV_USE_TABLE 1

/*================
 * Theme settings
 *================*/

/* A simple, impressive and very complete theme */
#define LV_USE_THEME_DEFAULT 1
#if LV_USE_THEME_DEFAULT
#define LV_THEME_DEFAULT_DARK 1 // <-- Your UI appears to use a dark theme
#define LV_THEME_DEFAULT_FONT &LV_FONT_DEFAULT
#endif

/*==================
 * Misc settings
 *==================*/

/* Enable API to take snapshot from the screen*/
#define LV_USE_SNAPSHOT 1

/* Add a custom tick source.
 * It's recommended to use a custom tick source with FreeRTOS. */
#define LV_TICK_CUSTOM 1 // <-- IMPORTANT: We will call lv_tick_inc() from our main loop

/*==================
 * Logging
 *==================*/
#define LV_USE_LOG 1
#if LV_USE_LOG
/*Can be LV_LOG_LEVEL_ERROR, LV_LOG_LEVEL_WARN, LV_LOG_LEVEL_INFO, LV_LOG_LEVEL_USER*/
#define LV_LOG_LEVEL LV_LOG_LEVEL_INFO
#endif

/*==================
 * Asserts
 *==================*/
#define LV_USE_ASSERT_NULL 1   /*Check if pointers are NULL*/
#define LV_USE_ASSERT_MALLOC 1 /*Check if malloc failed*/

/*==================
 * OS integration
 *==================*/

/* Enable OS integration with FreeRTOS */
#define LV_USE_OS LV_OS_FREERTOS // <-- For thread safety with WiFi/Webserver

/*====================
 * COMPILER SETTINGS
 *====================*/

/* For big endian hardware, set to 1 */
#define LV_BIG_ENDIAN_SYSTEM 0

/*===================
 * LVGL v9 DEMOS
 *==================*/
#define LV_USE_DEMO_WIDGETS 0
#define LV_USE_DEMO_BENCHMARK 0
#define LV_USE_DEMO_STRESS 0

#endif /*LV_CONF_H*/